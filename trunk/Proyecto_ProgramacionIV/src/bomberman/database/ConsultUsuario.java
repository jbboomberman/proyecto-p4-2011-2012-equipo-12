package bomberman.database;

public class ConsultUsuario {
	/*
	 * Este método será utilizado para realizar las consultas que se originen
	 * desde la ventana VentanaPuntuaciones, como podemos ver desde esta ventana
	 * se pueden hacer multitud de diferentes búsquedas a la base de datos y
	 * todos ellas son de tipo inclusión de tal manera que si rellenamos el nick
	 * del jugador y la fecha se buscará alguna puntuación que tenga ambos datos
	 * y no sólo uno de ellos. Para cada tipo de consulta que pueda generarse
	 * habrá un método específico ya que no puede crearse un método global.
	 */
}
