package bomberman.database;

public class AccesoNivel {
/*Representará el acceso a la tabla NIVEL y tendrá tres métodos
estáticos insertarNivel(Nivel niv) que insertará un nivel en la tabla, eliminarNivel(Nivel
niv) que eliminará un nivel de la tabla y listaNiv() que listará todos los niveles de la tabla. */
}
